یک پیچیدگی اضافی به‌روزرسانی صفحات در محل این است که کنترل همزمانی دقیق
مورد نیاز است اگر چندین نخ قرار است همزمان به درخت B دسترسی داشته باشند—در غیر این صورت یک نخ ممکن است
درخت را در یک حالت ناسازگار ببیند. این معمولاً با محافظت از ساختارهای داده درخت
با چفت‌ها (قفل‌های سبک‌وزن) انجام می‌شود. رویکردهای با ساختار لاگ از این نظر ساده‌تر هستند،
زیرا آنها تمام ادغام را در پس‌زمینه بدون تداخل با پرس‌وجوهای ورودی انجام می‌دهند و
گاهی اوقات قطعات قدیمی را به صورت اتمی با قطعات جدید جابجا می‌کنند. ### بهینه‌سازی‌های درخت B 
از آنجا که درخت‌های B مدت زیادی است که وجود دارند، تعجب‌آور نیست که بسیاری از بهینه‌سازی‌ها در طول
سال‌ها توسعه یافته‌اند. برای ذکر فقط چند مورد: *  
به جای بازنویسی صفحات و نگهداری یک WAL برای بازیابی از خرابی، برخی پایگاه‌های داده (مانند LMDB)
از یک طرح کپی-هنگام-نوشتن استفاده می‌کنند [[21](ch03.html#Chu2014we)].
یک صفحه اصلاح شده به یک مکان متفاوت نوشته می‌شود، و یک نسخه جدید از صفحات والد در درخت
ایجاد می‌شود، که به مکان جدید اشاره می‌کند. این رویکرد همچنین برای کنترل همزمانی مفید است، همانطور که
در ["ایزوله‌سازی اسنپ‌شات و خواندن تکرارپذیر"](ch07.html#sec_transactions_snapshot_isolation) خواهیم دید.